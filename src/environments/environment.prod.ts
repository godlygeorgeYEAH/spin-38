export const environment = {
  production: true,

  /**
   * URL base del backend (producción)
   * Esta URL será la del mismo servidor que sirve el iframe
   * Usar rutas relativas para evitar problemas de CORS
   */
  apiUrl: '/api'
};
